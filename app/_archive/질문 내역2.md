### 질문 내역2.

```bash
현재 디자인을 봤을 때 어떤지 ux 관점에서 상세히 분석해서 설명해주고 수정했으면 하는 부분이 있으면 나한테 건의해줘
```

```bash
⏺ 코드 기반으로 전체 UI 흐름을 파악했습니다. 페이지별로 UX
  분석합니다.

  ---
  UX 분석

  🔴 Critical

  1. EquipmentRow - 이중 border + 클릭 영역 버그

  <li className="... border border-white/10 bg-black/10 p-3">
   ← border + padding
    <button className="... border p-3 text-left">
  ← 또 border + padding

  - li의 p-3 영역(바깥 테두리 안쪽)은 클릭 안 됨 — button이
  안에 있으므로
  - border가 두 겹으로 시각적으로 두꺼워 보임
  - CLAUDE.md "border 중복 사용 금지" 위반

  ---
  🟠 High

  2. 탭 버튼 색상 문제 (CharacterDetailsClient.tsx)

  <div className="inline-flex rounded-xl bg-gray-600 p-1">
    <button className={tab === "overview" ? "bg-background
  text-foreground ..." : ...}>

  - bg-background는 shadcn 기본 CSS 변수 — 라이트 모드 기본값이
   #ffffff에 가까움
  - 다크 테마 고정 앱에서 활성 탭이 밝게 튀거나 오히려 안 보일
  수 있음
  - bg-gray-600 컨테이너도 다크 디자인 시스템(bg-[#212225],
  bg-zinc-950)과 톤이 맞지 않음

  3. 뒤로 가기 내비게이션 없음

  - /char/[name]에 진입하면 메인으로 돌아가는 UI 요소가 전혀
  없음
  - 브라우저 뒤로가기만 가능 → 모바일에서 특히 치명적
  - 헤더나 breadcrumb 자체가 없음

  4. 캐릭터 상세 h1이 단순 레이블

  <h1 className="text-2xl font-semibold">캐릭터 : {name}</h1>

  - "캐릭터 : 캐릭터명" — 콜론 앞뒤 공백이 어색
  - 서버명, 직업, 아이템 레벨 같은 핵심 정보가 API 응답 후에야
  ProfilePanel에 나타남
  - 탭과 h1 사이에 mt-? 간격이 없어서 붙어 보임

  ---
  🟡 Medium

  5. 탭 상태가 URL에 없음

  - "원정대" 탭에서 새로고침하면 "능력치" 탭으로 돌아감
  - URL 공유 불가 (?tab=siblings 같은 쿼리스트링 없음)

  6. 아이템 타입 텍스트 가독성

  <span className="... text-[8px] text-white">
    {item.type === "어빌리티 스톤" ? "스톤" : item.type}
  </span>

  - 8px는 아이콘 위에서 사실상 안 읽힘
  - "스톤"으로 줄이긴 했지만 "투구", "상의" 같은 건 그냥 표시 —
   일관성 없음

  7. EquipmentDetailDialog의 <pre> 태그

  function Section({ title, body }: { title: string; body:
  string }) {
    ...
    <pre className="mt-2 whitespace-pre-wrap
  text-sm">{body}</pre>

  - CLAUDE.md "pre 태그로 raw 텍스트 표시 금지" 위반
  - 폰트가 monospace로 렌더링되어 게임 스탯 텍스트가
  부자연스럽게 보임

  8. 메인 페이지 dead code

  // page.tsx
  const { data, isLoading, isError, error } =
  useSiblings(submittedName);
  // ↑ 받아서 아무것도 렌더링 안 함 (lint warning도 발생)

  - CharacterSearchForm에서 router.push로 바로 이동하므로
  useSiblings 결과가 쓸모없음
  - 불필요한 API 호출 발생

  ---
  🟢 Low / 체감 개선

  9. ProfilePanel 이미지 없을 때 레이아웃 변경

  - CharacterImage가 null이면 이미지 영역이 아예 사라짐 →
  텍스트 위치가 달라짐
  - 이미지 슬롯을 placeholder로 예약해두거나 fallback 아이콘
  사용 권장

  10. 로딩 상태가 분리되어 표시

  - ProfilePanel이 따로 로딩, EquipmentPanel이 따로 로딩 →
  화면이 순차적으로 채워지며 덜컹거림
  - 전체 skeleton을 한 번에 보여주는 게 더 자연스러움

  11. 검색 폼 - 초기화 버튼 위계

  - 검색 버튼과 초기화 버튼이 같은 높이/크기 →
  primary/secondary 구분 없음
  - 초기화를 실수로 누를 위험

  ---
  건의 우선순위 정리

  우선순위: P0
  항목: EquipmentRow 이중 border + 클릭 영역 수정
  작업 크기: S
  ────────────────────────────────────────
  우선순위: P1
  항목: 탭 버튼 색상 다크 테마에 맞게 수정
  작업 크기: S
  ────────────────────────────────────────
  우선순위: P1
  항목: /char/[name] 뒤로가기 링크/버튼 추가
  작업 크기: S
  ────────────────────────────────────────
  우선순위: P1
  항목: EquipmentDetailDialog <pre> → 스타일드 컴포넌트
  작업 크기: S
  ────────────────────────────────────────
  우선순위: P2
  항목: 메인 페이지 dead code(useSiblings) 제거
  작업 크기: XS
  ────────────────────────────────────────
  우선순위: P2
  항목: 탭 상태 URL 쿼리스트링 반영
  작업 크기: M
  ────────────────────────────────────────
  우선순위: P3
  항목: 아이템 타입 뱃지 가독성 개선
  작업 크기: S
  ────────────────────────────────────────
  우선순위: P3
  항목: 로딩 skeleton 통합
  작업 크기: M

  어느 항목부터 작업할지 알려주시면 바로 진행하겠습니다.

```

### 디자인 1

- 수정대로 변경하니 이전 디자인이 더 깔끔해보임
- 대신 테두리 영역에 클릭범위를 확장해야하는가? 그렇다면 확장을 하는 방향으로 수정
- 하지만 기존대로 카드 외곽 여백(padding)에는 클릭이 안 되는 디자인을 의도적 사용

```bash
우선순위: P0
항목: EquipmentRow 이중 border + 클릭 영역 수정
작업 크기: S

  이거 수정하고 싶은데 적용하지말고 코드로만 설명해주고
  클릭 영역 수정은 어떤 걸 의미하는거야?

  ⎿  ⧉ Selected 106 lines from
     app/components/equipment/EquipmentRow.tsx in VisualStudioCode


⏺ 현재 구조 문제 설명

  ┌─────────────────────────────┐  ← li (border + p-3)
  │  12px                       │     ← 이 영역 클릭 안 됨 ❌
  │  ┌───────────────────────┐  │
  │  │  button (border + p-3)│  │  ← 실제 클릭되는 영역만 여기
  │  │   content...          │  │
  │  └───────────────────────┘  │
  │  12px                       │
  └─────────────────────────────┘

  클릭 영역 수정이란 — li의 p-3 패딩 부분(외곽 테두리 ~ 버튼
  사이)을 마우스로 클릭해도 아무 반응이 없다는 뜻입니다.
  onClick이 button에만 붙어 있기 때문입니다.

  ---
  수정 코드

  li에서 스타일을 모두 제거하고, button이 전체를 채우도록
  합니다.

  // Before
  <li className="flex items-center gap-3 rounded-xl border
  border-white/10 bg-black/10 p-3">
    <button
      type="button"
      onClick={onClick}
      className="relative flex w-full items-center rounded-xl
  border p-3 text-left"
    >

  // After
  <li>
    <button
      type="button"
      onClick={onClick}
      className="relative flex w-full items-center gap-3
  rounded-xl border border-white/10 bg-black/10 p-3 text-left"
    >

  변경 포인트:
  - li → 빈 시맨틱 래퍼만 (스타일 없음)
  - button → li에 있던 gap-3, border-white/10, bg-black/10 흡수
  - button의 기존 border(색 없는 것) 제거 → border
  border-white/10 하나로 통일
  - hover 효과도 자연스럽게 button 전체에 적용 가능해짐

  ┌─────────────────────────────┐  ← button (border + p-3)
  │  content...                 │     ← 전체 영역 클릭 됨 ✅
  └─────────────────────────────┘
```

### 디자인 2

```bash
현재 문제:
  - bg-gray-600 → 다크 테마 톤과 안 맞음
  - bg-background / text-foreground → CSS 변수가 라이트 모드
  기준

  <div className="inline-flex rounded-xl bg-black/30 p-1">
    <button
      type="button"
      onClick={() => setTab("overview")}
      className={`
        px-4 py-2 rounded-lg text-sm transition-all
  duration-200 ease-out
        ${
          tab === "overview"
            ? "bg-zinc-700 text-zinc-100 font-medium shadow-sm"
            : "text-zinc-400 hover:text-zinc-200"
        }
      `}
    >
      능력치
    </button>

    <button
      type="button"
      onClick={() => setTab("siblings")}
      className={`
        px-4 py-2 rounded-lg text-sm transition-all
  duration-200 ease-out
        ${
          tab === "siblings"
            ? "bg-zinc-700 text-zinc-100 font-medium shadow-sm"
            : "text-zinc-400 hover:text-zinc-200"
        }
      `}
    >
      원정대
    </button>
  </div>

  변경 요약:
  - bg-gray-600 → bg-black/30
  - bg-background → bg-zinc-700
  - text-foreground → text-zinc-100
  - text-foreground/60 → text-zinc-400
```

## 디자인 2

탭 기능 다크모드에 맡게 색상 변경
